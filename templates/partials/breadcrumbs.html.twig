<!-- Start Banner Area -->
    <section class="banner-area organic-breadcrumb">
        <div class="container">
            <div class="breadcrumb-banner d-flex flex-wrap align-items-center justify-content-end">
                <div class="col-first">
                    <h1>{{name}}</h1>
                    <nav class="d-flex align-items-center">
                        <a href="{{path('app_home')}}">Accueil<span class="lnr lnr-arrow-right"></span></a>
                        <a href="#">{{name}}</a>
                    </nav>
                </div>
            </div>
        </div>
    </section>
    <!-- End Banner Area -->